# 赛博风识别单手写数字AI

requirements.txt为依赖

icon为图标

model为存档模型（数据正确率97%~~，但体验感好像不是这样的~~）

file中有data(测试数据，训练数据太大上传不了），以及神经网络py,训练py,实现py,使用流程 python3 train.py（或者可以不执行，只要你吧存档按我这样放） + python3 main.py

下载的只有Linux deb安装包，暂不支持windows
